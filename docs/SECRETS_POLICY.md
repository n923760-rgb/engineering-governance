# Secrets Policy

Secrets must not appear in Git, task prompts, AI reports, shared screenshots, shell history when avoidable, CI logs, evidence archives, or PR comments.

Use secret stores, environment injection, owner-entered credentials, least-privilege credentials, and repository-scoped access.

Reports may include a secret's logical name and rotation state, never its value.
